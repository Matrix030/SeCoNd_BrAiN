_>type statusCode int

instead of using the normal int type we create our own type so that it is easy to read and convert when necessary and is overall  a better choice when we have to use the update function