---
tags: [book, os, operating-systems, book-os-concepts, networking]
aliases: ["Client-Server Computing"]
---

# Client-Server Computing

1) As PCs have become faster, more powerful, and cheaper, designers have shifted away from centralized system architecture. 
2) Terminals connected to centralized systems are now being supplanted by PCs. 
3) Correspondingly, user-interface functionality once handled directly by centralized systems is increasingly being handled by PCs. 
4) As a result, many of today’s systems act as server systems to satisfy requests generated by **client systems**. 
5) This form of specialized distributed system, called a **client-server** system, has the general structure depicted in the Figure.
![[Pasted image 20260527165701.png]]
6) Server systems can be broadly categorized as compute servers and file servers:
	• The **compute-server system** provides an interface to which a client can send a request to perform an action (for example, read data); in response, the server executes the action and sends back results to the client. 
	• The **file-server system** provides a file-system interface where clients can create, update, read, and delete files. An example of such a system is a Web server that delivers files to clients running Web browsers.

## Related

- [[> Computing Environments]] — back to the sub-topic MOC
- [[Peer-to-Peer Computing]] — a distributed model with no client/server distinction
- [[> Distributed Systems]] — client-server is a specialized distributed system
