---
tags: [book, os, operating-systems, book-os-concepts, moc]
aliases: ["Basic Concepts"]
---

# Basic Concepts — Map of Content

> [!info] Section 5.1
> The basic concepts behind CPU scheduling — why multiprogramming needs it, the alternating CPU–I/O burst cycle, and the components that carry out scheduling.

---

## Notes

- [[Multiprogramming]] — why CPU scheduling exists: keeping a process running to maximize CPU utilization
- [[CPU-I/O Burst Cycle]] — process execution as an alternating cycle of CPU bursts and I/O waits
- [[CPU Scheduler]] — the short-term scheduler that picks a process from the ready queue
- [[Preemptive Scheduling]] — the four scheduling circumstances, and preemptive vs nonpreemptive schemes
- [[Dispatcher]] — the module that hands the CPU to the selected process, and dispatch latency

---

## Related

- [[> CPU Scheduling]] — back to the chapter MOC
