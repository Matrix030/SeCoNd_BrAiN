---
tags: [book, os, operating-systems, book-os-concepts, processes, threads, moc]
aliases: ["Thread Libraries"]
---

# Thread Libraries — Map of Content

> [!info] Section 4.3
> The APIs for creating and managing threads — how a thread library is implemented (user space vs kernel space) and basic thread creation in the three main libraries: Pthreads, Win32, and Java, all illustrated with a non-negative integer summation program.

---

## Notes

- [[Thread Library Implementation]] — user-space vs kernel-space libraries, the three main libraries, and the running summation example
- [[Pthreads]] — the POSIX thread API and its summation program in C
- [[Win32 Threads]] — the Windows kernel-level thread API and its summation program in C
- [[Java Threads]] — threads in the JVM via the Runnable interface and the Thread class

---

## Related

- [[> Threads]] — back to the chapter MOC
